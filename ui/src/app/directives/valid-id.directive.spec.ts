import { ValidIDDirective } from './valid-id.directive';

describe('ValidIDDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidIDDirective();
    expect(directive).toBeTruthy();
  });
});
