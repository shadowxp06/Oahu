export interface MessageCounts {
  all: number;
  announcements: number;
  unread: number;

  globalAll: number;
  globalAnnouncements: number;
  globalUnread: number;
  globalDrafts: number;
  globalStarred: number;
}
